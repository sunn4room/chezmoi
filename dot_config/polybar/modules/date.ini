[module/date]
type = internal/date
interval = 1

time = "%H:%M"
time-alt = "%Y %b %d %H:%M:%S %A"

format = <label>
format-prefix = "%{T2}ﲊ%{T-}"
format-prefix-foreground = ${color.background}
format-prefix-background = ${color.blue}

label = "%time%"
label-padding = 1
label-foreground = ${color.foreground}
label-background = ${color.background}

